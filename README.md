Debug code for unpacking nested arrays/slices in Go-Ethereum.

# Get Started

Re-generate bindings with: `go generate` (See command in `main.go` file header)

Test/debug code is located in `bind_nested_array/`.

